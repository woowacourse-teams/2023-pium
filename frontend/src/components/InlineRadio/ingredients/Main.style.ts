import { styled } from 'styled-components';

export const Wrapper = styled.span`
  display: inline-flex;
  font-size: 1.8rem;
`;

export const Division = styled.p`
  display: inline-flex;
  align-items: center;
`;
