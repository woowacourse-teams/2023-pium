import { styled } from 'styled-components';

export const Label = styled.label`
  cursor: pointer;
  display: 'flex';
  gap: '12px';
  align-items: 'center';
`;

export const Input = styled.input`
  display: none;
  padding: 0;
`;
