import { styled } from 'styled-components';

export const Label = styled.label`
  display: 'flex';
  gap: '12px';
  align-items: 'center';
  &:hover {
    cursor: pointer;
  }
`;

export const Input = styled.input`
  display: none;
  padding: 0;
`;
